# CI/CD Pipeline for resource-service
# Triggers only when backend/resource-service changes on main branch

name: Deploy Resource Service

on:
  push:
    branches:
      - main
    paths:
      - 'backend/resource-service/**'
  workflow_dispatch: # allows manual trigger

env:
  IMAGE_NAME: resource-service
  ACR_NAME: malindadisaregistry
  RESOURCE_GROUP: disa-rg
  CONTAINER_APP_NAME: resource-service

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1 - Checkout code
      - name: Checkout code
        uses: actions/checkout@v4

      # Step 2 - Login to Azure Container Registry
      - name: Login to ACR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.ACR_NAME }}.azurecr.io
          username: ${{ secrets.RESOURCE_ACR_USERNAME }}
          password: ${{ secrets.RESOURCE_ACR_PASSWORD }}

      # Step 3 - Build and push Docker image
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: backend/resource-service
          file: backend/resource-service/Dockerfile
          push: true
          tags: |
            ${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:${{ github.run_number }}
            ${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:latest

      # Step 4 - Login to Azure and deploy
      - name: Login to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.RESOURCE_AZURE_CREDENTIALS }}

      # Step 5 - Deploy to Container Apps
      - name: Deploy to Container Apps
        run: |
          az containerapp update \
            --name ${{ env.CONTAINER_APP_NAME }} \
            --resource-group ${{ env.RESOURCE_GROUP }} \
            --image ${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:${{ github.run_number }}
          az containerapp ingress update \
            --name ${{ env.CONTAINER_APP_NAME }} \
            --resource-group ${{ env.RESOURCE_GROUP }} \
            --target-port 8089
